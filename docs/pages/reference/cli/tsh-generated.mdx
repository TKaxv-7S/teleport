---
title: tsh Reference
description: Provides a comprehensive list of commands, arguments, and flags for tsh.
---

This guide provides a comprehensive list of commands, arguments, and flags for
tsh.

```code
$ tsh [<flags>] <command> [<args> ...]
```

Teleport Command Line Client

Global flags:

|Flag|Description|
|---|---|
|-l, --login=LOGIN|Remote host login ($TELEPORT_LOGIN)||    --proxy=PROXY|Teleport proxy address ($TELEPORT_PROXY)||    --user=USER|Teleport user, defaults to current local user ($TELEPORT_USER)||    --ttl=TTL|Minutes to live for a session||-i, --identity=IDENTITY|Identity file ($TELEPORT_IDENTITY_FILE)||    --cert-format=CERT-FORMAT|SSH certificate format||    --[no-]insecure|Do not verify server's certificate and host name. Use only in test environments||    --auth=AUTH|Specify the name of authentication connector to use. ($TELEPORT_AUTH)||    --[no-]skip-version-check|Skip version checking between server and client.||-d, --[no-]debug|Verbose logging to stdout||-k, --add-keys-to-agent="auto"|Controls how keys are handled. Valid values are [auto no yes only]. ($TELEPORT_ADD_KEYS_TO_AGENT)||    --[no-]enable-escape-sequences|Enable support for SSH escape sequences. Type '~?' during an SSH session to list supported sequences. Default is enabled.||    --bind-addr=BIND-ADDR|Override host:port used when opening a browser for cluster logins ($TELEPORT_LOGIN_BIND_ADDR)||    --callback=CALLBACK|Override the base URL (host:port) of the link shown when opening a browser for cluster logins. Must be used with --bind-addr.||    --mfa-mode=auto|Preferred mode for MFA and Passwordless assertions (auto, cross-platform, platform, otp, sso) ($TELEPORT_MFA_MODE)||    --[no-]headless|Use headless login. Shorthand for --auth=headless. ($TELEPORT_HEADLESS)||    --mlock="auto"|Determines whether process memory will be locked and whether failure to do so will be accepted (off, auto, best_effort, strict). ($TELEPORT_MLOCK_MODE)||    --piv-slot=PIV-SLOT|Specify a PIV slot key to use for Hardware Key support instead of the default. Ex: "9d" ($TELEPORT_PIV_SLOT)||-J, --jumphost=JUMPHOST|SSH jumphost|

## tsh help

Show help.

Usage:

```code
$ tsh help [<command>...]
```
## tsh version

Print the tsh client and Proxy server versions for the current context.

Usage:

```code
$ tsh version [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --[no-]client|Show the client version only (no server required).|## tsh ssh

Run shell or execute a command on a remote SSH node.

Usage:

```code
$ tsh ssh [<flags>] [<[user@]host>] [<command>...]
```

Flags:

|Flag|Description|
|---|---|
|-p, --port=PORT|SSH port on a remote host||-A, --[no-]forward-agent|Forward agent to target node||-L, --forward=FORWARD ...|Forward localhost connections to remote server||-D, --dynamic-forward=DYNAMIC-FORWARD ...|Forward localhost connections to remote server using SOCKS5||-R, --remote-forward=REMOTE-FORWARD ...|Forward remote connections to localhost||    --[no-]local|Execute command on localhost after connecting to SSH node||-t, --[no-]tty|Allocate TTY||-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||-o, --option=OPTION ...|OpenSSH options in the format used in the configuration file||-N, --[no-]no-remote-exec|Don't execute remote command, useful for port forwarding||-X, --[no-]x11-untrusted|Requests untrusted (secure) X11 forwarding for this session||-Y, --[no-]x11-trusted|Requests trusted (insecure) X11 forwarding for this session. This can make your local machine vulnerable to attacks, use with caution||    --x11-untrusted-timeout=10m|Sets a timeout for untrusted X11 forwarding, after which the client will reject any forwarding requests from the server||    --invite=INVITE ...|A comma separated list of people to mark as invited for the session.||    --reason=REASON|The purpose of the session.||    --[no-]participant-req|Displays a verbose list of required participants in a moderated session.||    --request-reason=REQUEST-REASON|Reason for requesting access||    --request-mode=resource|Type of automatic access request to make (off, resource, role) ($TELEPORT_REQUEST_MODE)||    --[no-]disable-access-request|Disable automatic resource access requests (DEPRECATED: use --request-mode=off)||    --log-dir=LOG-DIR|Directory to log separated command output, when executing on multiple nodes. If set, output from each node will also be labeled in the terminal.||    --[no-]no-resume|Disable SSH connection resumption ($TELEPORT_NO_RESUME)||    --[no-]relogin|Permit performing an authentication attempt on a failed command|## tsh resolve

Resolves an SSH host.

Usage:

```code
$ tsh resolve [<flags>] <host>
```

Flags:

|Flag|Description|
|---|---|
|-q, --[no-]quiet|Quiet mode||-f, --format=text|Format output (text, json, yaml)|## tsh aws

Access AWS API.

Usage:

```code
$ tsh aws [<flags>] [<command>...]
```

Flags:

|Flag|Description|
|---|---|
|    --app=APP|Optional Name of the AWS application to use if logged into multiple.||    --exec=EXEC|Execute different commands (e.g. terraform) under Teleport credentials||    --aws-role=AWS-ROLE|(For AWS CLI access only) Amazon IAM role ARN or role name.|## tsh az

Access Azure API.

Usage:

```code
$ tsh az [<flags>] [<command>...]
```

Flags:

|Flag|Description|
|---|---|
|--app=APP|Optional name of the Azure application to use if logged into multiple.||--azure-identity=AZURE-IDENTITY|(For Azure CLI access only) Azure managed identity name.|## tsh gcloud

Access GCP API with the gcloud command.

Usage:

```code
$ tsh gcloud [<flags>] [<command>...]
```

Flags:

|Flag|Description|
|---|---|
|--app=APP|Optional name of the GCP application to use if logged into multiple.||--gcp-service-account=GCP-SERVICE-ACCOUNT|(For GCP CLI access only) GCP service account name.|## tsh gsutil

Access Google Cloud Storage with the gsutil command.

Usage:

```code
$ tsh gsutil [<flags>] [<command>...]
```

Flags:

|Flag|Description|
|---|---|
|--app=APP|Optional name of the GCP application to use if logged into multiple.||--gcp-service-account=GCP-SERVICE-ACCOUNT|(For GCP CLI access only) GCP service account name.|## tsh apps ls

List available applications.

Usage:

```code
$ tsh apps ls [<flags>] [<labels>]
```

Flags:

|Flag|Description|
|---|---|
|-v, --[no-]verbose|Show extra application fields.||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-f, --format=text|Format output (text, json, yaml)||-R, --[no-]all|List apps from all clusters and proxies.|## tsh apps login

Retrieve short-lived certificate for an app.

Usage:

```code
$ tsh apps login [<flags>] <app>
```

Flags:

|Flag|Description|
|---|---|
|    --aws-role=AWS-ROLE|(For AWS CLI access only) Amazon IAM role ARN or role name.||    --azure-identity=AZURE-IDENTITY|(For Azure CLI access only) Azure managed identity name.||    --gcp-service-account=GCP-SERVICE-ACCOUNT|(For GCP CLI access only) GCP service account name.||    --target-port=TARGET-PORT|Port to which connections made using this cert should be routed to. Valid only for multi-port TCP apps.||-q, --[no-]quiet|Quiet mode|## tsh apps logout

Remove app certificate.

Usage:

```code
$ tsh apps logout [<app>]
```
## tsh apps config

Print app connection information.

Usage:

```code
$ tsh apps config [<flags>] [<app>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=FORMAT|Optional print format, one of: "uri" to print app address, "ca" to print CA cert path, "cert" to print cert path, "key" print key path, "curl" to print example curl command, "json" or "yaml" to print everything as JSON or YAML.|## tsh recordings ls

List recorded sessions.

Usage:

```code
$ tsh recordings ls [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml). Defaults to 'text'.||    --from-utc=FROM-UTC|Start of time range in which recordings are listed. Format 2006-01-02. Defaults to 24 hours ago.||    --to-utc=TO-UTC|End of time range in which recordings are listed. Format 2006-01-02. Defaults to current time.||    --limit=50|Maximum number of recordings to show. Default 50.||    --last=LAST|Duration into the past from which session recordings should be listed. Format 5h30m40s|## tsh recordings export

Export recorded desktop sessions to video.

Usage:

```code
$ tsh recordings export [<flags>] <session-id>
```

Flags:

|Flag|Description|
|---|---|
|--out=OUT|Override output file name|## tsh proxy ssh

Start local TLS proxy for ssh connections when using Teleport in single-port
mode.

Usage:

```code
$ tsh proxy ssh [<flags>] <[user@]host>
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --[no-]no-resume|Disable SSH connection resumption ($TELEPORT_NO_RESUME)||    --[no-]relogin|Permit performing an authentication attempt on a failed command|## tsh proxy db

Start local TLS proxy for database connections when using Teleport in
single-port mode.

Usage:

```code
$ tsh proxy db [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|-p, --port=PORT|Specifies the source port used by proxy db listener||    --[no-]tunnel|Open authenticated tunnel using database's client certificate so clients don't need to authenticate||-u, --db-user=DB-USER|Database user to log in as.||-n, --db-name=DB-NAME|Database name to log in to.||-r, --db-roles=DB-ROLES|List of comma separate database roles to use for auto-provisioned user.||-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||    --request-reason=REQUEST-REASON|Reason for requesting access||    --[no-]disable-access-request|Disable automatic resource access requests|## tsh proxy app

Start local TLS proxy for app connection when using Teleport in single-port
mode.

Usage:

```code
$ tsh proxy app [<flags>] <app>
```

Flags:

|Flag|Description|
|---|---|
|-p, --port=PORT|Specifies the listening port used by by the proxy app listener. Accepts an optional target port of a multi-port TCP app after a colon, e.g. "1234:5678"||-c, --cluster=CLUSTER|Specify the Teleport cluster to connect|## tsh proxy aws

Start local proxy for AWS access.

Usage:

```code
$ tsh proxy aws [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|    --app=APP|Optional Name of the AWS application to use if logged into multiple.||-p, --port=PORT|Specifies the source port used by the proxy listener.||-f, --format=unix|Optional format to print the commands for setting environment variables, one of: unix, command-prompt, powershell, text. Default is unix. Or specify a service format, one of: athena-odbc, athena-jdbc|## tsh proxy azure

Start local proxy for Azure access.

Usage:

```code
$ tsh proxy azure [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|    --app=APP|Optional Name of the Azure application to use if logged into multiple.||-p, --port=PORT|Specifies the source port used by the proxy listener.||-f, --format=unix|Optional format to print the commands for setting environment variables, one of: unix, command-prompt, powershell, text. Default is unix.|## tsh proxy gcloud

Start local proxy for GCP access.

Usage:

```code
$ tsh proxy gcloud [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|    --app=APP|Optional Name of the GCP application to use if logged into multiple.||-p, --port=PORT|Specifies the source port used by the proxy listener.||-f, --format=unix|Optional format to print the commands for setting environment variables, one of: unix, command-prompt, powershell, text. Default is unix.|## tsh proxy kube

Start local proxy for Kubernetes access.

Usage:

```code
$ tsh proxy kube [<flags>] [<kube-cluster>...]
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --as=AS|Configure custom Kubernetes user impersonation.||    --as-groups=AS-GROUPS ...|Configure custom Kubernetes group impersonation.||-n, --namespace=NAMESPACE|Configure the default Kubernetes namespace.||-p, --port=PORT|Specifies the source port used by the proxy listener||-f, --format=unix|Optional format to print the commands for setting environment variables, one of: unix, command-prompt, powershell, text. Default is unix.||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||    --set-context-name="{{.ClusterName}}-{{.KubeName}}"|Define a custom context name or template.||    --[no-]exec|Run the proxy in the background and reexec into a new shell with $KUBECONFIG already pointed to our config file.|## tsh db ls

List all available databases.

Usage:

```code
$ tsh db ls [<flags>] [<labels>]
```

Flags:

|Flag|Description|
|---|---|
|-v, --[no-]verbose|Show extra database fields.||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-f, --format=text|Format output (text, json, yaml)||-R, --[no-]all|List databases from all clusters and proxies.|## tsh db login

Retrieve credentials for a database.

Usage:

```code
$ tsh db login [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-u, --db-user=DB-USER|Database user to configure as default.||-n, --db-name=DB-NAME|Database name to configure as default.||-r, --db-roles=DB-ROLES|List of comma separate database roles to use for auto-provisioned user.||    --request-reason=REQUEST-REASON|Reason for requesting access||    --[no-]disable-access-request|Disable automatic resource access requests|## tsh db logout

Remove database credentials.

Usage:

```code
$ tsh db logout [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|--labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||--query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')|## tsh db env

Print environment variables for the configured database.

Usage:

```code
$ tsh db env [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')|## tsh db config

Print database connection information. Useful when configuring GUI clients.

Usage:

```code
$ tsh db config [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-f, --format=FORMAT|Print format: "text" to print in table format (default), "cmd" to print connect command, "json" or "yaml" to print in JSON or YAML.|## tsh db connect

Connect to a database.

Usage:

```code
$ tsh db connect [<flags>] [<db>]
```

Flags:

|Flag|Description|
|---|---|
|-u, --db-user=DB-USER|Database user to log in as.||-n, --db-name=DB-NAME|Database name to log in to.||-r, --db-roles=DB-ROLES|List of comma separate database roles to use for auto-provisioned user.||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||    --request-reason=REQUEST-REASON|Reason for requesting access||    --[no-]disable-access-request|Disable automatic resource access requests|## tsh join

Join the active SSH or Kubernetes session.

Usage:

```code
$ tsh join [<flags>] <session-id>
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||-m, --mode=observer|Mode of joining the session, valid modes are observer, moderator and peer.|## tsh play

Replay the recorded session (SSH, Kubernetes, App, DB).

Usage:

```code
$ tsh play [<flags>] <session-id>
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --speed=1x|Playback speed, applicable when streaming SSH or Kubernetes sessions.||    --[no-]skip-idle-time|Quickly skip over idle time, applicable when streaming SSH or Kubernetes sessions.||-f, --format=pty|Format output (pty, json, yaml, text)|## tsh scp

Transfer files to a remote SSH node.

Usage:

```code
$ tsh scp [<flags>] <from, to>...
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||-r, --[no-]recursive|Recursive copy of subdirectories||-P, --port=PORT|Port to connect to on the remote host||-p, --[no-]preserve|Preserves access and modification times from the original file||-q, --[no-]quiet|Quiet mode||    --[no-]no-resume|Disable SSH connection resumption ($TELEPORT_NO_RESUME)||    --[no-]relogin|Permit performing an authentication attempt on a failed command|## tsh ls

List remote SSH nodes.

Usage:

```code
$ tsh ls [<flags>] [<labels>]
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||-v, --[no-]verbose|One-line output (for text format), including node UUIDs||-f, --format=text|Format output (text, json, yaml, names)||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-R, --[no-]all|List nodes from all clusters and proxies.|## tsh clusters

List available Teleport clusters.

Usage:

```code
$ tsh clusters [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||-q, --[no-]quiet|Quiet mode||-v, --[no-]verbose|Verbose table output, shows full label output|## tsh sessions ls

List active sessions.

Usage:

```code
$ tsh sessions ls [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --kind=ssh... ...|Filter by session kind(s)|## tsh login

Log in to a cluster and retrieve the session certificate.

Usage:

```code
$ tsh login [<flags>] [<cluster>]
```

Flags:

|Flag|Description|
|---|---|
|-o, --out=OUT|Identity output||-f, --format="file"|Identity format: file, openssh (for OpenSSH compatibility) or kubernetes (for kubeconfig)||    --[no-]overwrite|Whether to overwrite the existing identity file.||    --request-roles=REQUEST-ROLES|Request one or more extra roles||    --request-reason=REQUEST-REASON|Reason for requesting additional roles||    --request-reviewers=REQUEST-REVIEWERS|Suggested reviewers for role request||    --[no-]request-nowait|Finish without waiting for request resolution||    --request-id=REQUEST-ID|Login with the roles requested in the given request||    --browser=BROWSER|Set to 'none' to suppress browser opening on login||    --kube-cluster=KUBE-CLUSTER|Name of the Kubernetes cluster to login to||-v, --[no-]verbose|Show extra status information|## tsh logout

Delete a cluster certificate.

Usage:

```code
$ tsh logout
```
## tsh latency ssh

Measure latency to a particular SSH host.

Usage:

```code
$ tsh latency ssh [<flags>] <[user@]host>
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --[no-]no-resume|Disable SSH connection resumption ($TELEPORT_NO_RESUME)|## tsh status

Display the list of proxy servers and retrieved certificates.

Usage:

```code
$ tsh status [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||-v, --[no-]verbose|Show extra status information after successful login|## tsh env

Print commands to set Teleport session environment variables.

Usage:

```code
$ tsh env [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --[no-]unset|Print commands to clear Teleport session environment variables|## tsh request ls

List access requests.

Usage:

```code
$ tsh request ls [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --[no-]reviewable|Only show requests reviewable by current user||    --[no-]suggested|Only show requests that suggest current user as reviewer||    --[no-]my-requests|Only show requests created by current user|## tsh request show

Show request details.

Usage:

```code
$ tsh request show [<flags>] <request-id>
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)|## tsh request create

Create a new access request.

Usage:

```code
$ tsh request create [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--roles=ROLES|Roles to be requested||--reason=REASON|Reason for requesting||--reviewers=REVIEWERS|Suggested reviewers||--[no-]nowait|Finish without waiting for request resolution||--resource=RESOURCE ...|Resource ID to be requested||--request-ttl=REQUEST-TTL|Expiration time for the access request||--session-ttl=SESSION-TTL|Expiration time for the elevated certificate||--max-duration=MAX-DURATION|How long the access should be granted for||--assume-start-time=ASSUME-START-TIME|Sets time roles can be assumed by requestor (RFC3339 e.g 2023-12-12T23:20:50.52Z)|## tsh request review

Review an access request.

Usage:

```code
$ tsh request review [<flags>] <request-id>
```

Flags:

|Flag|Description|
|---|---|
|--[no-]approve|Review proposes approval||--[no-]deny|Review proposes denial||--reason=REASON|Review reason message||--assume-start-time=ASSUME-START-TIME|Sets time roles can be assumed by requestor (RFC3339 e.g 2023-12-12T23:20:50.52Z)|## tsh request search

Search for resources to request access to.

Usage:

```code
$ tsh request search --kind=KIND [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|    --kind=KIND|Resource kind to search for (node, kube_cluster, db, app, windows_desktop, user_group, pod, secret, configmap, namespace, service, serviceaccount, kube_node, persistentvolume, persistentvolumeclaim, deployment, replicaset, statefulset, daemonset, clusterrole, kube_role, clusterrolebinding, rolebinding, cronjob, job, certificatesigningrequest, ingress, saml_idp_service_provider, aws_ic_account, aws_ic_account_assignment)||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --kube-cluster=KUBE-CLUSTER|Kubernetes Cluster to search for Pods||    --namespace="default"|Kubernetes Namespace to search for Pods||    --[no-]all-kube-namespaces|Search Pods in every namespace||-v, --[no-]verbose|Verbose table output, shows full label output|## tsh request drop

Drop one more access requests from current identity.

Usage:

```code
$ tsh request drop [<request-id>...]
```
## tsh headless approve

Approve a headless authentication request.

Usage:

```code
$ tsh headless approve [<flags>] [<request id>]
```

Flags:

|Flag|Description|
|---|---|
|--[no-]skip-confirm|Skip confirmation and prompt for MFA immediately ($TELEPORT_HEADLESS_SKIP_CONFIRM)|## tsh kubectl

Runs a kubectl command on a Kubernetes cluster.

Usage:

```code
$ tsh kubectl [<>...]
```
## tsh kube ls

Get a list of Kubernetes clusters.

Usage:

```code
$ tsh kube ls [<flags>] [<labels>]
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||-f, --format=text|Format output (text, json, yaml)||-R, --[no-]all|List Kubernetes clusters from all clusters and proxies.||-v, --[no-]verbose|Show an untruncated list of labels.||-q, --[no-]quiet|Quiet mode.|## tsh kube login

Login to a Kubernetes cluster.

Usage:

```code
$ tsh kube login [<flags>] [<kube-cluster>]
```

Flags:

|Flag|Description|
|---|---|
|-c, --cluster=CLUSTER|Specify the Teleport cluster to connect||    --labels=LABELS|List of comma separated labels to filter by labels (e.g. key1=value1,key2=value2)||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')||    --as=AS|Configure custom Kubernetes user impersonation.||    --as-groups=AS-GROUPS ...|Configure custom Kubernetes group impersonation.||-n, --namespace=NAMESPACE|Configure the default Kubernetes namespace.||    --[no-]all|Generate a kubeconfig with every cluster the user has access to. Mutually exclusive with --labels or --query.||    --set-context-name="{{.ClusterName}}-{{.KubeName}}"|Define a custom context name. To use it with --all include "{{.KubeName}}"||    --request-reason=REQUEST-REASON|Reason for requesting access||    --[no-]disable-access-request|Disable automatic resource access requests|## tsh kube sessions

Get a list of active Kubernetes sessions. (DEPRECATED: use tsh sessions ls
--kind=kube instead)

Usage:

```code
$ tsh kube sessions [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||-c, --cluster=CLUSTER|Specify the Teleport cluster to connect|## tsh kube exec

Execute a command in a Kubernetes pod.

Usage:

```code
$ tsh kube exec [<flags>] <target> <command>...
```

Flags:

|Flag|Description|
|---|---|
|-c, --container=CONTAINER|Container name. If omitted, use the kubectl.kubernetes.io/default-container annotation for selecting the container to be attached or the first container in the pod will be chosen||-n, --namespace=NAMESPACE|Configure the default Kubernetes namespace.||-f, --filename=FILENAME|to use to exec into the resource||-q, --[no-]quiet|Only print output from the remote session||-s, --[no-]stdin|Pass stdin to the container||-t, --[no-]tty|Stdin is a TTY||    --reason=REASON|The purpose of the session.||    --invite=INVITE|A comma separated list of people to mark as invited for the session.||    --[no-]participant-req|Displays a verbose list of required participants in a moderated session.|## tsh kube join

Join an active Kubernetes session.

Usage:

```code
$ tsh kube join [<flags>] <session>
```

Flags:

|Flag|Description|
|---|---|
|-m, --mode=observer|Mode of joining the session, valid modes are observer, moderator and peer.||-c, --cluster=CLUSTER|Specify the Teleport cluster to connect|## tsh mfa ls

Get a list of registered MFA devices.

Usage:

```code
$ tsh mfa ls [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-v, --[no-]verbose|Print more information about MFA devices||-f, --format=text|Format output (text, json, yaml)|## tsh mfa add

Add a new MFA device.

Usage:

```code
$ tsh mfa add [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--name=NAME|Name of the new MFA device||--type=TYPE|Type of the new MFA device (TOTP, WEBAUTHN)|## tsh mfa rm

Remove a MFA device.

Usage:

```code
$ tsh mfa rm <name>
```
## tsh scan keys

Scan the local machine for SSH private keys and report findings to Teleport.

Usage:

```code
$ tsh scan keys [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--dirs=/Users/ ...|Directories to scan.||--skip-paths=SKIP-PATHS ...|Paths to directories or files to skip. Supports for matching patterns.|## tsh config

Print OpenSSH configuration details.

Usage:

```code
$ tsh config [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|-p, --port=PORT|SSH port on a remote host|## tsh device enroll

Enroll this device as a trusted device. Requires Teleport Enterprise.

Usage:

```code
$ tsh device enroll [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--[no-]current-device|Attempts to register and enroll the current device. Requires device admin privileges.||--token=TOKEN|Device enrollment token|## tsh svid issue

Issue a SPIFFE SVID using Teleport Workload Identity and write it to a local
directory.

Usage:

```code
$ tsh svid issue --output=OUTPUT [<flags>] <path>
```

Flags:

|Flag|Description|
|---|---|
|--type=x509|Type of the SVID to issue (x509). Defaults to x509.||--output=OUTPUT|Path to the directory to write the SVID into.||--dns-san=DNS-SAN ...|DNS SANs to include in the SVID. By default, none are included.||--ip-san=IP-SAN ...|IP SANs to include in the SVID. By default, none are included.||--svid-ttl=1h|Sets the time to live for the SVID.|## tsh workload-identity issue-x509

Use Teleport Workload Identity to issue an X509 credential write it to a local
directory.

Usage:

```code
$ tsh workload-identity issue-x509 --output=OUTPUT [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--name-selector=NAME-SELECTOR|The name of the workload identity to issue||--label-selector=LABEL-SELECTOR|A label-based selector for which workload identities to issue. Multiple labels can be provided using ','.||--credential-ttl=1h|Sets the time to live for the credential.||--output=OUTPUT|Path to the directory to write the SVID into.|## tsh vnet

Start Teleport VNet, a virtual network for TCP application access.

Usage:

```code
$ tsh vnet [<flags>]
```

Flags:

|Flag|Description|
|---|---|
## tsh git login

Opens a browser and retrieves your login from GitHub.

Usage:

```code
$ tsh git login --github-org=GITHUB-ORG [<flags>]
```

Flags:

|Flag|Description|
|---|---|
|--github-org=GITHUB-ORG|GitHub organization||--[no-]force|Force a login|## tsh git ls

List Git servers.

Usage:

```code
$ tsh git ls [<flags>] [<labels>]
```

Flags:

|Flag|Description|
|---|---|
|-f, --format=text|Format output (text, json, yaml)||    --search=SEARCH|List of comma separated search keywords or phrases enclosed in quotations (e.g. --search=foo,bar,"some phrase")||    --query=QUERY|Query by predicate language enclosed in single quotes. Supports ==, !=, &&, and || (e.g. --query='labels["key1"] == "value1" && labels["key2"] != "value2"')|## tsh git config

Check Teleport config on the working Git directory. Or provide an action
('update' or 'reset') to configure the Git repo.

Usage:

```code
$ tsh git config [<action>]
```
## tsh git clone

Clone a Git repository.

Usage:

```code
$ tsh git clone <repository> [<directory>]
```

